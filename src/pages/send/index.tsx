import { Layout } from "../../components/layout"
import { SendOperations } from "../../components/send-operations"

export const Send = () => {
  return (
    <Layout>
      <SendOperations />
    </Layout>
  )
}
